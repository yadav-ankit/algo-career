Algo tips

Next greater element --> start from last index and keep checking stack ..agar mujhse chota h to ignore (pop it) otherwise stack.top is the NGE.
Next smaller element --> start from the first index and keep checking stack ..agar mujhse bda h to ignore (pop it) otherwise stack.top is the NSE
------------------------------------------------------------------------------------------------------------------
Tree tricks to rememeber

for Diameter of tree use the hegith method only ..just keeps a global max variable dia

dia = max(dia , left + right);


------------------------------------------------------------------------------------------------------------------

For sliding window
window length = r - l + 1; ( +1 bcz its 0 based indexing)

if u want to reach the first element of the window use (r-l) only.

------------------------------------------------------------------------------------------------------------------


For Print all paths use Strings not List bcz strings are immutable
every += creates a brand new String, it never modifies the original.

and for list
new ArrayList<>(path) takes a PHOTO at that moment
path = [1,2]  →  new ArrayList<>(path)  =  independent copy [1,2]
Even if path changes later, the copy is unaffected. That's the snapshot.

if u use List then u have to use ans.add(new ArrayList(path));
and then backtrack in the end like path.remove(path.size() -1);

------------------------------------------------------------------------------------------------------------------

Edit distance vs LCSubsequence vs LCs(Substring)

if (s1.charAt(i-1) == s2.charAt(j-1)) {
  dp[i][j] = dp[i-1][j-1]; // match: free
} else {
  dp[i][j] = 1 + Math.min(dp[i-1][j-1], // replace
  Math.min(dp[i-1][j], // delete s1[i]
  dp[i][j-1])); // insert s2[j]
}

LCS
if (s1.charAt(i-1) == s2.charAt(j-1))
  dp[i][j] = dp[i-1][j-1] + 1; // match: extend
else
  dp[i][j] = Math.max(dp[i-1][j], dp[i][j-1]);

LCs Track global max during fill — answer is NOT at dp[m][n].
if (s1.charAt(i-1) == s2.charAt(j-1)) {
  dp[i][j] = dp[i-1][j-1] + 1;
  max = Math.max(max, dp[i][j]);
} else {
  dp[i][j] = 0; 
}

------------------------------------------------------------------------------------------------------------------


1. for LRU cache  = Map + Double LL
  - we store key as index or id and value as Node and in LL all are Nodes
  - Why can't i use just a LL bcz i want get in O(1) 
  - Why can't i use just a Map bcz i want to maintain order of elements(Node)
  -  Note that if i call remove(Node) i dont have to iterate the list ..since its a double LL so i will have node.prev and node.next , u can direclty remove in O(1)
  - So you have to do removeLast( ) and addFront( )
  - LinkedHashMap with ordered = true is predefined LRU from java (u dont need LL here)

------------------------------------------------------------------------------------------------------------------

2. Kakfa Exactly once

At-Most-Once
The producer sends a message and does not wait for confirmation.

At-Least-Once
The producer sends a message and waits for the broker to acknowledge it.

If the acknowledgment does not arrive (network failure, broker crash, timeout), the producer retries. This guarantees the message is not lost, but the retry might create a duplicate if the original write actually succeeded and only the acknowledgment was lost.

Exactly once
 1) Idempotent Producers enable.idempotence=true
Every message the producer sends includes the PID and a sequence number that increments with each message, per partition.

The broker tracks the last sequence number it received from each producer for each partition. When a new message arrives, the broker checks: is this sequence number exactly one more than the last one I saw?

If yes, the message is new and is written.

If no (because it is a retry of an already-written message), the broker acknowledges the write without actually writing a duplicate.

2) Transactions
    producer.beginTransaction();
    
    producer.send(new ProducerRecord<>("topic-A", "A"));
    producer.send(new ProducerRecord<>("topic-B", "B"));
    
    producer.commitTransaction();


                      Producer
                        │
                        │ transactional.id=P1
                        ▼
                Transaction Coordinator
                     Broker 3
                    /         \
                   /           \
                  ▼             ▼
             Broker 1        Broker 7
             Topic A         Topic B

The producer calls commitTransaction() or abortTransaction().

The coordinator writes a commit marker to all partitions involved in the transaction.

Consumers with read_committed isolation only see messages up to the last commit marker.
